# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust
target

# Local iLEAPP engine (set up via scripts/setup-engine.sh)
/engine

# Claude Code local runtime & personal config — agent worktrees, locks, and
# settings.local.json. Shared, committable agent config lives in .agents/
# (skills), so the whole .claude/ folder is local-only. If a shared .claude/
# file is ever needed (e.g. a project settings.json or a /command), add a
# matching `!.claude/<path>` negation below.
.claude/
*.bak
